<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ingot.cloud.pms.mapper.SysRoleOauthClientMapper">

    <insert id="insertIgnore">
        INSERT IGNORE INTO sys_role_oauth_client(role_id, client_id)
        VALUES (#{roleId}, #{clientId})
    </insert>

    <select id="getRoleClients" resultType="com.ingot.cloud.pms.api.model.domain.Oauth2RegisteredClient">
        SELECT client.*
        FROM oauth2_registered_client AS client
            <if test="isBind">
                INNER JOIN sys_role_oauth_client AS rc ON rc.client_id = client.id
            </if>
        <where>
            <if test="isBind">
                rc.role_id = #{roleId}
            </if>
            <if test="condition.clientId != null and condition.clientId != ''">
                AND LOCATE(#{condition.clientId}, client.client_id) > 0
            </if>
        </where>
        ORDER BY client.client_id_issued_at ASC
    </select>
</mapper>
