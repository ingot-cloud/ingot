apply from: "${project.getRootDir().path}/config/library-base.gradle"

dependencies {
    implementation(deps.spring.spring_boot.spring_boot_autoconfigure)
    api(deps.spring.security_config)
    compileOnly(deps.spring.security_web)
}
