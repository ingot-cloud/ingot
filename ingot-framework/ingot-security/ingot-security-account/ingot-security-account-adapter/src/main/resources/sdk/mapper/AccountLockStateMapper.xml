<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ingot.framework.security.account.adapter.mapper.AccountLockStateMapper">

    <!--
        原子 upsert：不存在时插入（failed_login_count=1），存在时原子递增。
        依赖唯一键 uk_user_id_type (user_id, user_type)。
    -->
    <insert id="upsertIncrementFailCount">
        INSERT INTO account_lock_state
            (user_id, user_type, locked, failed_login_count, last_failed_at, created_at, updated_at)
        VALUES
            (#{userId}, #{userType}, 0, 1, #{now}, #{now}, #{now})
        ON DUPLICATE KEY UPDATE
            failed_login_count = failed_login_count + 1,
            last_failed_at     = #{now},
            updated_at         = #{now}
    </insert>

    <!-- 直接将失败计数清零，无需先 SELECT -->
    <update id="resetFailCountDirect">
        UPDATE account_lock_state
        SET failed_login_count = 0,
            last_failed_at     = NULL,
            updated_at         = #{now}
        WHERE user_id   = #{userId}
          AND user_type = #{userType}
    </update>

    <select id="findExpiredLocksByPage" resultType="com.ingot.framework.security.account.adapter.entity.AccountLockStateEntity">
        SELECT *
        FROM account_lock_state
        WHERE locked = 1
          AND locked_until IS NOT NULL
          AND locked_until &lt;= #{now}
          AND id &gt; #{afterId}
        ORDER BY id ASC
        LIMIT #{pageSize}
    </select>

</mapper>
