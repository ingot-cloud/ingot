# ====================================
# Docker Compose 环境变量配置
# 针对 8核16G 服务器优化（默认推荐配置）
# 使用方法: cp env-templates/env.8c16g.template .env
# ====================================

# ========== 镜像仓库配置 ==========
REGISTRY_URL=docker-registry.ingotcloud.top

# ========== Docker 基础配置 ==========
DOCKER_VOLUME=/ingot-data

# ========== Gateway 配置 ==========
GATEWAY_VERSION=0.1.0
GATEWAY_PORT=7980
GATEWAY_CPUS=1.5
GATEWAY_MEMORY=2.5g
VIRTUAL_HOST=ingot-api.ingotcloud.top
# Swarm 部署生效，部署节点数量
GATEWAY_REPLICAS=1

# ========== Auth 服务配置 ==========
AUTH_VERSION=0.1.0
AUTH_CPUS=2
AUTH_MEMORY=3g
# Swarm 部署生效，部署节点数量
AUTH_REPLICAS=1

# ========== Member 服务配置 ==========
MEMBER_VERSION=0.1.0
MEMBER_CPUS=2
MEMBER_MEMORY=3g
# Swarm 部署生效，部署节点数量
MEMBER_REPLICAS=1

# ========== IAM 服务配置 ==========
IAM_VERSION=0.1.0
# 显式填写已验证的 IAM 目标库名，不得填旧源库。
IAM_DATABASE=
IAM_CPUS=2
IAM_MEMORY=3g
# Swarm 部署生效，部署节点数量
IAM_REPLICAS=1

# ====================================
# 资源分配说明（8核16G）
# ====================================
# CPU: Auth(2) + Gateway(1.5) + Member(2) + IAM(2) = 7.5核
# 内存: Auth(3G) + Gateway(2.5G) + Member(3G) + IAM(3G) + 系统(4.5G) = 16G
# ====================================
