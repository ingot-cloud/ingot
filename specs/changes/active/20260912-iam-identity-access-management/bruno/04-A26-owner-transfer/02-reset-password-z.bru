meta {
  name: 重置 Z 密码
  type: http
  seq: 2
}

post {
  url: {{gateway}}/iam/v1/platform/accounts/{{accountZId}}/reset-password
  body: json
  auth: bearer
}

auth:bearer {
  token: {{tokenPlatform}}
}

body:json {
  {
    "expectedVersion": "{{accountZVersion}}"
  }
}

script:post-response {
  const data = res.getBody().data;
  if (data && data.password) bru.setEnvVar("accountZPassword", data.password);

}

tests {
  test("Z 密码已返回", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
