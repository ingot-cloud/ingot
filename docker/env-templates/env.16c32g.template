# ====================================
# Docker Compose 环境变量配置
# 针对 16核32G 服务器优化（生产环境）
# 使用方法: cp env-templates/env.16c32g.template .env
# ====================================

# ========== 镜像仓库配置 ==========
REGISTRY_URL=docker-registry.ingotcloud.top
VERSION=0.1.0

# ========== Docker 基础配置 ==========
DOCKER_VOLUME=/ingot-data

# ========== Gateway 配置 ==========
GATEWAY_PORT=7980
GATEWAY_REPLICAS=1
GATEWAY_CPUS=2
GATEWAY_MEMORY=5g
VIRTUAL_HOST=ingot-api.ingotcloud.top

# ========== Auth 服务配置 ==========
AUTH_REPLICAS=1
AUTH_CPUS=3
AUTH_MEMORY=6g

# ========== Member 服务配置 ==========
MEMBER_REPLICAS=1
MEMBER_CPUS=3
MEMBER_MEMORY=6g

# ========== PMS 服务配置 ==========
PMS_REPLICAS=1
PMS_CPUS=3
PMS_MEMORY=6g

# ====================================
# 资源分配说明（16核32G）
# ====================================
# CPU: Auth(3) + Gateway(2) + Member(3) + PMS(3) = 11核
# 内存: Auth(6G) + Gateway(5G) + Member(6G) + PMS(6G) + 系统(9G) = 32G
# ====================================
