# ====================================
# Docker Compose 环境变量配置
# 针对 8核16G 服务器优化（默认推荐配置）
# 使用方法: cp env-templates/env.8c16g.template .env
# ====================================

# ========== 镜像仓库配置 ==========
REGISTRY_URL=docker-registry.ingotcloud.top
VERSION=0.1.0

# ========== Docker 基础配置 ==========
DOCKER_VOLUME=/ingot-data

# ========== Gateway 配置 ==========
GATEWAY_PORT=7980
GATEWAY_REPLICAS=1
GATEWAY_CPUS=1.5
GATEWAY_MEMORY=2.5g
VIRTUAL_HOST=ingot-api.ingotcloud.top

# ========== Auth 服务配置 ==========
AUTH_REPLICAS=1
AUTH_CPUS=2
AUTH_MEMORY=3g

# ========== Member 服务配置 ==========
MEMBER_REPLICAS=1
MEMBER_CPUS=2
MEMBER_MEMORY=3g

# ========== PMS 服务配置 ==========
PMS_REPLICAS=1
PMS_CPUS=2
PMS_MEMORY=3g

# ====================================
# 资源分配说明（8核16G）
# ====================================
# CPU: Auth(2) + Gateway(1.5) + Member(2) + PMS(2) = 7.5核
# 内存: Auth(3G) + Gateway(2.5G) + Member(3G) + PMS(3G) + 系统(4.5G) = 16G
# ====================================
