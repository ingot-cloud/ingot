# ====================================
# Docker Compose 环境变量配置
# 针对 4核8G 服务器优化（开发/测试环境）
# 使用方法: cp env-templates/env.4c8g.template .env
# ====================================

# ========== 镜像仓库配置 ==========
REGISTRY_URL=docker-registry.ingotcloud.top

# ========== Docker 基础配置 ==========
DOCKER_VOLUME=/ingot-data

# ========== Gateway 配置 ==========
GATEWAY_VERSION=0.1.0
GATEWAY_PORT=7980
GATEWAY_CPUS=1
GATEWAY_MEMORY=1.5g
VIRTUAL_HOST=ingot-api.ingotcloud.top
# Swarm 部署生效，部署节点数量
GATEWAY_REPLICAS=1

# ========== Auth 服务配置 ==========
AUTH_VERSION=0.1.0
AUTH_CPUS=1
AUTH_MEMORY=1.5g
# Swarm 部署生效，部署节点数量
AUTH_REPLICAS=1

# ========== Member 服务配置 ==========
MEMBER_VERSION=0.1.0
MEMBER_CPUS=1
MEMBER_MEMORY=1.5g
# Swarm 部署生效，部署节点数量
MEMBER_REPLICAS=1

# ========== PMS 服务配置 ==========
PMS_VERSION=0.1.0
PMS_CPUS=1
PMS_MEMORY=1.5g
# Swarm 部署生效，部署节点数量
PMS_REPLICAS=1

# ====================================
# 资源分配说明（4核8G）
# ====================================
# CPU: Auth(1) + Gateway(1) + Member(1) + PMS(1) = 4核
# 内存: Auth(1.5G) + Gateway(1.5G) + Member(1.5G) + PMS(1.5G) + 系统(2G) = 8G
# ====================================
